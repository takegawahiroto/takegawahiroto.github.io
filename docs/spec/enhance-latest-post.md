# 最新ブログ記事の内容拡充

## 背景・目的

`20260308-latest.md` は「AIによるセルフトラッキング・自己分析」をテーマにしているが、現状はアイデアの概要に留まっている。以下のセクションを追加し、読み応えのある記事にする。

## 追加内容

### 1. Quantified Self 2.0 — LLM時代のセルフトラッキング

従来のQuantified Self（歩数、睡眠時間など数値的トラッキング）と、LLMを使った質的データの分析（Slackの会話トーン、購入パターンからの性格推定など）の違いを整理する。LLMによって「構造化されていないデータ」が分析対象になった点を強調。

### 2. アーキテクチャ概要 — 画面トラッキングツールの仕組み

筆者が作っている画面トラッキングツールの簡易的なアーキテクチャを説明するセクション。スクリーンショット取得→OCR/マルチモーダルLLMで内容認識→カテゴリ分類→日次レポート生成、という流れをテキストで解説。

### 3. プライバシーとのトレードオフ

自分のデータを自分のために分析する場合でも、データの保存場所・LLM APIへの送信・家族や同僚の情報が含まれるリスクなど、考慮すべき点を短く述べる。

### 4. 次のステップ — 「観察」から「介入」へ

現状は「記録して可視化する」段階だが、将来的には「集中力が落ちてきたら通知する」「週次で改善提案を出す」など、AIが能動的にフィードバックする仕組みへの展望。

## 実装方針

- 変更対象: `src/content/posts/20260308-latest.md` のみ
- カテゴリを "AI" に変更（内容的にAIカテゴリが適切）
- tagsに "Quantified Self", "Productivity" を追加

## テスト計画

- Markdownコンテンツの変更のみのため、`npm run build` が成功することを確認

## 影響範囲

- ブログ記事1つの内容変更のみ。他ページへの影響なし。
